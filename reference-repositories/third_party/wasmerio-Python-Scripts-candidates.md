# wasmerio / Python-Scripts — Candidate Scripts

Suggested candidate scripts to index and review for agent/tooling reuse:

- `Web Scraper` — HTTP scraping patterns and HTML parsing helpers
- `FileOrganizer` / `FileRenamer` / `Duplicate Finder` — file-system automation patterns
- `CSV to Excel` / `JSON to CSV` — data conversion helpers for ingestion pipelines
- `Run Then Notify` — long-running job + notification pattern
- `Automated Emails` / `Mail Sender` — email automation examples (careful with credentials)
- `Screenshot to Text` / `Image to PDF` — OCR and document conversion examples
- `YouTube Downloader` / `Video Downloader` — media ingestion examples (lawful use only)

Next steps:

1. After cloning, list these folders and capture README snippets into `LEARNING_SUMMARY.md`.
2. Create sandboxed wrappers under `tools/` that adapt safe examples for demos.
